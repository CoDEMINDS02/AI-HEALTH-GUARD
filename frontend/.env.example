# Base URL for API requests from the React/Vite frontend.
# When the frontend and backend are deployed together on Vercel (same project),
# API calls use the same origin and the /api path is handled by the FastAPI
# serverless function. Set this in the Vercel dashboard.
VITE_API_BASE_URL=/api
