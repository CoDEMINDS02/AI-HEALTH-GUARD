# URL of the deployed FastAPI backend (Render, Railway, etc.)
# Set this in the Vercel dashboard under Environment Variables → Production.
# Include the /api prefix. Do NOT add a trailing slash.
# Leave unset for local development — the Vite dev proxy handles /api automatically.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
