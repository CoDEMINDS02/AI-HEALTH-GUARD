# Production default for Vercel Services (same-origin) deployment.
# The frontend and backend share the same origin, so API calls use the /api path.
# This is a non-secret build-time default. Override in Vercel dashboard if needed.
VITE_API_BASE_URL=/api
